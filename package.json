{
  "private": true,
  "version": "1.0.0",
  "packageManager": "pnpm@7.1.6",
  "scripts": {
    "build": "pnpm --filter=@matrix/* run build",
    "changeset": "changeset",
    "version-packages": "changeset version"
  },
  "engines": {
    "node": ">=16.15.0"
  },
  "devDependencies": {
    "@changesets/cli": "^2.22.0"
  }
}
